type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  pw: String!
}

type Philosopher {
  id: ID! @unique
  name: String! @unique
  imgURL: String!
  timePeriod: String!
  region: String
  branch: String
  resource: String!
}
